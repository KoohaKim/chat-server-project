package com.example.gooha;

import org.springframework.transaction.annotation.Transactional;

/*
public class XXService {
    public void test(){
        bool success = aServer.httppostpurchase();
        if(!success) {
            throw exception();
        }

        bool success2 = bServer.httpostpointadd();
        if(!success2){
            aServer.httpdeletepurchase();

            return fail;
        }

        return ok;
    }

    @Transactional
    public void test2(){
        bool success = aServer.httppostpurchase();
        if(!success) {
            throw exception();
        }

        myDatabase.addDXXXX();
        bool success2 = bServer.httpostpointadd();
        if(!success2){
            // kafkaBroker.sendPointAddFailEvent();
            myDatabase.saveSendPointAddFailEvent();

            return fail;
        }

        return ok;
    }

}

 */
